# 퀀트 포트폴리오 일봉 데이터 저장 현황 분석

**확인 날짜**: 2025-12-16

---

## 📊 현재 상황

### 확인 결과
- **퀀트 포트폴리오 종목 수**: 10개
- **일봉 데이터 저장**: 10/10개 (100%)
- **저장 테이블**: `daily_prices`

### 문제점
- 퀀트 포트폴리오는 **최대 30개** 종목을 선정해야 함
- 현재는 **10개만** 선정되어 있음

---

## 🔍 원인 분석

### 1. 퀀트 스크리닝 vs ML 스크리닝
- **퀀트 스크리닝** (`_run_quant_screening`): `portfolio_size=30` (30개 선정)
- **ML 스크리닝** (`_run_ml_screening`): `top_n=10` (10개만 선정)

### 2. 현재 실행 중인 스크리닝
- 현재 DB에 10개만 저장되어 있는 것으로 보아, **ML 스크리닝이 실행**되었거나
- 퀀트 스크리닝이 실행되지 않았을 가능성이 있음

---

## ✅ 일봉 데이터 저장 로직 확인

### 현재 구현 상태
1. **퀀트 스크리닝 완료 후** (`main.py:967-986`)
   - 선정된 종목을 `intraday_manager.add_selected_stock()`으로 추가
   - 각 종목에 대해 일봉 데이터 수집 및 DB 저장

2. **일봉 데이터 수집** (`core/intraday_stock_manager.py:197-257`)
   - `_collect_daily_data_only()`: 일봉 데이터 수집
   - `_save_daily_to_db()`: `daily_prices` 테이블에 저장

3. **DB 저장** (`core/ml_data_collector.py:43-120`)
   - `_save_daily_prices_to_db()`: `daily_prices` 테이블에 INSERT OR REPLACE

### 결론
✅ **로직은 이미 구현되어 있음**
- 퀀트 포트폴리오 선정 종목에 대한 일봉 데이터 수집 및 저장 로직이 정상 작동 중
- 현재 10개 종목 모두 일봉 데이터가 저장됨

---

## 🎯 해결 방안

### 1. 퀀트 스크리닝 실행 확인
- 퀀트 스크리닝이 제대로 실행되었는지 확인
- 로그에서 "퀀트 스크리닝 완료" 메시지 확인

### 2. ML 스크리닝 설정 변경 (선택사항)
- ML 스크리닝의 `top_n`을 30으로 변경하거나
- 퀀트 스크리닝만 사용하도록 설정

### 3. 퀀트 포트폴리오 선정 종목 수 확인
- `quant_portfolio` 테이블에서 오늘 날짜의 종목 수 확인
- 30개가 선정되었는지 확인

---

## 📝 권장 사항

1. ✅ **현재 로직은 정상 작동 중**
   - 퀀트 포트폴리오 선정 종목에 대한 일봉 데이터 수집 및 저장 로직이 구현되어 있음
   - 현재 10개 종목 모두 일봉 데이터가 저장됨

2. ⚠️ **퀀트 스크리닝 실행 확인 필요**
   - 퀀트 스크리닝이 제대로 실행되어 30개 종목이 선정되었는지 확인
   - 로그에서 퀀트 스크리닝 실행 여부 확인

3. 🔧 **설정 확인**
   - `main.py`에서 퀀트 스크리닝과 ML 스크리닝 중 어느 것이 실행되는지 확인
   - 두 스크리닝이 모두 실행되는 경우, 우선순위 확인

---

## 📊 요약

- ✅ **일봉 데이터 저장 로직**: 정상 작동
- ✅ **현재 저장된 종목**: 10/10개 (100%)
- ⚠️ **퀀트 포트폴리오 종목 수**: 10개 (목표: 30개)
- 🔍 **원인**: 퀀트 스크리닝이 실행되지 않았거나, ML 스크리닝이 먼저 실행됨

---

**보고서 생성 시간**: 2025-12-16


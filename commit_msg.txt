safety_add_final_fill_check_on_timeout_path

- Add _final_fill_check_on_timeout() method as safety net
- On timeout cancel failure, check if order was actually filled via API
- If filled, save to DB and trigger callbacks (prevents missing SELL records)
- Handle market price orders (price=0) by falling back to current price
- Apply same safety net to 4-candle timeout path
- Addresses issue where filled orders were marked as TIMEOUT without DB record

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
